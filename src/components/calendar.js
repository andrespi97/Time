import React from "react";

const Calendar = ({ calendarId }) => {
  // Aquí debes agregar la lógica para mostrar el calendario basado en el calendarId
  return (
    <div>
      <h3>Calendar {calendarId}</h3>
    </div>
  );
};

export default Calendar;
